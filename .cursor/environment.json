{
  "install": "bash .cursor/scripts/cloud-install.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd backend && source .venv/bin/activate && PYTHONPATH=.. uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
    },
    {
      "name": "web",
      "command": "cd frontend && npm run dev -- --hostname 0.0.0.0 --port 3000"
    }
  ]
}
